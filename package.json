{
  "name": "cjlint-sarif-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@swc/core": "^1.15.0",
    "typescript": "^5.9.0"
  }
}
